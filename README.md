1. **To build the place from scratch, execute**:

```bash
rojo build -o "project-boilerplate.rbxlx"
```

2. **Afterward, open `project-boilerplate.rbxlx` in Roblox Studio and initiate the Rojo server**:

```bash
rojo serve
```